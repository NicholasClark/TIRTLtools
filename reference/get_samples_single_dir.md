# Internal helper for `get_samples()`

same as
[`get_samples()`](https://nicholasclark.github.io/TIRTLtools/reference/get_samples.md),
but it takes in one directory only

## Usage

``` r
get_samples_single_dir(data_dir, pattern = NULL)
```
